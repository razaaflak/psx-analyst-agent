# Weekly Review — 2026-W26 (weekend of 2026-06-28)

**Mode: UNIVERSE PREDICTION ENGINE** (post-2026-06-24 pivot). The portfolio loop is removed; this
review is about the *engine's* plumbing and training readiness, not a paper book. The legacy
portfolio-era `weekly_run.md` steps (counterfactuals, holdings thesis) are N/A in engine mode.

## TL;DR
- **Are we improving?** Structurally, yes — the #1 build blocker is now cleared. The universe grader
  is wired: `grade_predictions.py --universe` mechanically scores `universe_signals.csv` with no
  look-ahead. Until this weekend the universe accrued signals it could never learn from.
- **Biggest lesson:** the "yield bug" (TODO #2) was a **misdiagnosis**, not a code bug. The `2.93`
  that triggered it was the **momentum** column being read as `yield_pct`. Yields are already
  face-value-correct (INDU 11.98%, FFC 8.14%, OGDC 5.59%, UBL 8.47%). Closed by verification, no edit.
- **Main risk next week:** the entire signal history is still **one regime** (melt-up). No real
  grades exist yet — first batch matures **2026-06-30**. Do not trust any hit-rate until a
  multi-session, multi-regime sample accrues.

---

## 1. What was built this weekend

### TODO #1 — universe grader (DONE) ✅
Extended `scripts/grade_predictions.py`:
- New `grade_universe(asof, write)` + `grade_universe_row()` + `atr14_at()`; CLI flags
  `--universe` and `--write`.
- Mapping per SKILL.md H5, scored from the raw OHLC store, **bars strictly after the signal date**
  through `grade_on_date`, on the adjusted-close basis (same basis as the stored signal `close`):
  - **BUY** → HIT if any in-window close > signal-date close.
  - **SELL** → HIT if any in-window close < signal-date close.
  - **HOLD** → HIT if every in-window close stays within ±1×ATR14 (computed no-look-ahead at the
    signal date), falling back to ±3% when ATR is unavailable.
- Writes `status` (`open`→`graded`) and a grade string into the `result` column **only**. The issued
  `why` (factor explanation) and all other issued fields are immutable. Defaults to a dry run;
  `--write` persists.

**Verification (no-look-ahead proven both ways):**
- Real run today (`--asof 2026-06-28`): **0 matured / 0 graded** — correct; nothing has matured.
- Dry-run preview (`--asof 2026-07-01`, not written): of 200 rows, the 06-23 batch graded (it has a
  post-signal bar at 06-24); the 06-24 batch returned **NEEDS-DATA** because the store has no bars
  after 06-24 yet. The grader refuses to grade without post-signal bars — exactly the guard we want.
- CSV confirmed untouched after the dry runs (all 200 rows still `open`, no git diff).

> ⚠ The 80% shown in the *preview* is a non-result artifact: the 06-23 window is a **single session**
> (06-24), not the intended ~5-session horizon. It was never written and is not a hit-rate. Ignore it.

### TODO #2 — "yield bug" (CLOSED — was a misdiagnosis) ✅
`run_state.md` claimed INDU showed 2.93% vs real ~9.5%. Traced it: in the INDU row,
`yield_pct=11.98` and `mom20=2.93`. The 2.93 is the **momentum** column. The yield math
(`ttm_dps` = Σ interim+final `pct`/100 × face, ÷ adjusted price) is already face-value-correct
(R-EXP-002). Spot-checked INDU/FFC/OGDC/FATIMA/UBL — all sane. **No code change; closed by evidence.**

---

## 2. Engine state & data
- **Rule scorecard:** LIVE; 200 signal rows (06-23, 06-24 × 100), all `status=open`.
- **Universe hit-rate:** **0 graded** (honestly UNGRADED). First batch (06-23) matures 06-30; second
  (06-24) matures 07-01. The grader is ready to score them once the OHLC store has the post-signal bars.
- **OHLC store:** ends **2026-06-24** (101 symbols, adjusted). Needs a daily `collect_ohlc.py --update`
  before the 06-30/07-01 grades can be computed.
- **ML model / ensemble (TODO #4/#5):** not started.
- **Regime:** still the single melt-up regime since ~06-09. Regime-stratified grading remains essential.

---

## 3. Week-ahead (engine-relevant)
- **2026-06-30:** first universe batch matures → run `collect_ohlc.py --update` for the week's bars,
  then `grade_predictions.py --universe --asof 2026-06-30 --write` for the FIRST real graded sample.
- **2026-07-01:** second batch matures; same procedure.
- Watch the **regime flip line:** KSE100 close < MA20 ends the melt-up — the most valuable
  out-of-regime grading data the engine can get. Watch oil (Brent ~$76, sinking) for the E&P-drag
  hypothesis (R-EXP-003).

## 4. Updated BUILD TODOs (priority)
1. ~~Grade `universe_signals.csv`~~ **DONE.** Next: run it for real once bars accrue (06-30/07-01).
2. ~~Yield bug~~ **CLOSED (misdiagnosis).**
3. **Rebuild rule evidence from the universe** — unblocked now; recompute each rule's hit-rate across
   all symbols where it fired, once a real graded sample exists.
4. **Build `scripts/universe_model.py`** (ML classifier) + ensemble + grading.
5. **Extend `build_db.py`** to ingest the signal logs (+ grade columns).

## What I'm getting better at next week
Turning the now-working grader into the first *honest* universe sample — wiring `--update` →
`--universe --write` into the daily loop so grades accrue mechanically, and resisting the temptation
to read the 1-session preview as a result.

---
*Not financial advice. This is a training engine — output is a graded prediction, never a trade recommendation.*
