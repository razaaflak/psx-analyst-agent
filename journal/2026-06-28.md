# Journal — 2026-06-28 (weekend review, W26) — ENGINE MODE

Weekend run. Engine mode (post-2026-06-24 pivot); portfolio-era weekly steps N/A. Goal this run:
the BUILD TODOs, especially the #1 blocker. Two closed.

## TODO #1 — universe grader: BUILT + VERIFIED
Extended `scripts/grade_predictions.py`:
- `grade_universe(asof, write)`, `grade_universe_row(row)`, `atr14_at(symbol, asof)`; CLI `--universe`, `--write`.
- SKILL.md H5 mapping, no look-ahead (bars strictly after signal date → grade_on_date), adjusted-close basis:
  BUY = any in-window close > signal-date close; SELL = any close < it; HOLD = within ±1×ATR14 (±3% fallback).
- Writes `status`→`graded` + grade into `result` only. Issued `why` + all issued fields immutable. Dry-run default.

Verification:
- `--asof 2026-06-28` → 200 rows, 0 matured, 0 graded (correct — nothing matured).
- `--asof 2026-07-01` preview (not written) → grades the 06-23 batch (post-signal bar exists at 06-24),
  NEEDS-DATA for the 06-24 batch (no bars after 06-24 in store). Refuses to grade without post-signal bars.
- CSV untouched after dry runs (200 rows still `open`, no git diff).
- ⚠ The preview "80%" is a 1-session-window artifact, never written, NOT a hit-rate.

Wired into `pipeline/daily_run.md` Step 2.

## TODO #2 — "yield bug": CLOSED (misdiagnosis)
`run_state` claimed INDU 2.93% vs real ~9.5%. The 2.93 is the **mom20** column, not `yield_pct`.
Actual yields face-value-correct: INDU 11.98%, FFC 8.14%, OGDC 5.59%, FATIMA 6.28%, UBL 8.47%.
No code change. Corrected the stale yield warning in `daily_run.md`.

## State
- 200 universe signal rows, all open. Universe hit-rate: 0 graded (honest UNGRADED).
- OHLC store ends 2026-06-24 → must `--update` before 06-30/07-01 grades.
- Still single melt-up regime. ML model / ensemble not built (TODO #4/#5).

## Next run
1. `collect_ohlc.py --update --universe` (get bars past 06-24).
2. On/after 06-30: `grade_predictions.py --universe --write` → first REAL graded sample (06-23 batch); 07-01 for 06-24 batch.
3. Then TODO #3 (rebuild rule evidence from universe), #4 (ML model), #5 (ensemble), #6 (build_db ingest).

*Not financial advice. Training engine — graded prediction, never a trade recommendation.*
